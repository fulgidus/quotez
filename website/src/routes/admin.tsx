export function AdminPage() {
  return (
    <div>
      <h1 style={{ marginBottom: 'var(--spacing-lg)' }}>Dashboard</h1>
      <p style={{ color: 'var(--color-text-muted)' }}>Welcome to the admin dashboard.</p>
    </div>
  );
}
